type Post {
  id: ID! @id
  published: Boolean! @default(value: false)
  title: String!
  content: String!
}

type ContactEntry {
  id: ID! @id
  name: String!
  phone: String!
  email: String!
  message: String!
}
